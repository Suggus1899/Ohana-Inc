services:
  postgres:
    image: postgres:16-alpine
    container_name: ohana-postgres-prod
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-ohana_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-ohana_password_2026}
      POSTGRES_DB: ${DB_NAME:-ohana_db}
    volumes:
      - pgdata_prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-ohana_user} -d ${DB_NAME:-ohana_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "${DB_PORT:-5433}:5432"
    networks:
      - ohana-network

  redis:
    image: redis:7-alpine
    container_name: ohana-redis-prod
    restart: always
    ports:
      - "${REDIS_PORT:-6381}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ohana-network

  backend:
    build:
      context: ../..
      dockerfile: apps/backend/docker/Dockerfile.prod
    image: ohana-backend-prod:latest
    container_name: ohana-backend-prod
    restart: always
    ports:
      - "${BACKEND_PORT:-3026}:5000"
    environment:
      PORT: 5000
      DB_HOST: postgres
      DB_PORT: 5432
      REDIS_URL: redis://redis:6379
      OSRM_API_URL: http://osrm:5000
      NOMINATIM_API_URL: http://osrm:8080
    env_file:
      - .env
    volumes:
      - storage_prod:/app/storage
      - logs_prod:/app/logs
      - ./uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - ohana-network

  osrm:
    build:
      context: ../..
      dockerfile: apps/backend/docker/Dockerfile.combined
    image: ohana-combined-prod:latest
    container_name: ohana-osrm-prod
    restart: always
    ports:
      - "${OSRM_PORT:-5001}:5000"
      - "${GEOCODE_PORT:-8082}:8080"
    volumes:
      - ./data:/data
    networks:
      - ohana-network

networks:
  ohana-network:
    driver: bridge

volumes:
  pgdata_prod:
  storage_prod:
  logs_prod:
